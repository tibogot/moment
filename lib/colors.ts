/** TextReveal blocks on cream page surfaces — follows the palette preview toggle. */
export const REVEAL_BLOCK = "var(--palette-reveal)";

/** TextReveal blocks on sky-tinted section surfaces — inverse of REVEAL_BLOCK. */
export const REVEAL_BLOCK_ON_SKY = "var(--palette-reveal-on-sky)";

/** Hero photography keeps cream blocks regardless of palette preview. */
export const REVEAL_HERO = "#f8f7f2";
